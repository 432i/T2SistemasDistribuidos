syntax = "proto3";
option go_package = ".;connclidn";
package connclidn;

message Chunk{
    bytes datos = 1;
}

message Message {
  string body = 1;
}

service ChatCliDn {
    rpc ChunkaDN(Chunk) returns (Message){}
}