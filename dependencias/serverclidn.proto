syntax = "proto3";
option go_package = ".;serverclidn";
package serverclidn;

message Chunk{
    bytes datos = 1;
}

message Message {
  string body = 1;
}

service ChatCliDn {
    rpc ChunkaDN(Chunk) returns (Message){}
}